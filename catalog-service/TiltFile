# catalog-service/Tiltfile

# PostgreSQL compartido (red omnicore-dev). Debe levantarse antes que catalog-api.
include('../infra/Tiltfile')

docker_compose('./docker-compose.dev.yml')  # pyright: ignore[reportUndefinedVariable]

dc_resource(  # pyright: ignore[reportUndefinedVariable]
  'catalog-api',
  resource_deps=['postgres'],
  labels=['catalog-service']
)
